services:
  bot:
    build: .
    container_name: telegram-notes-bot
    network_mode: host
    env_file: .env
    volumes:
      - /root/vault:/vault
      - /root/bot-data:/data
    restart: unless-stopped
